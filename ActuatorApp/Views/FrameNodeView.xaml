<UserControl x:Class="ActuatorApp.Views.FrameNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ActuatorApp.Views"
             xmlns:nodes="clr-namespace:ActuatorApp.ViewModels.Nodes"
             mc:Ignorable="d" 
             x:Name="Root"
             d:DesignHeight="200" d:DesignWidth="300">
    
    <Grid x:Name="MainContainer">
        <!-- Frame 背景 (半透明) -->
        <Border x:Name="BackgroundBorder"
                CornerRadius="8"
                Opacity="0.4">
            <Border.Background>
                <SolidColorBrush x:Name="BackgroundBrush" Color="#007ACC"/>
            </Border.Background>
        </Border>
        
        <!-- Frame 邊框 -->
        <Border x:Name="BorderElement"
                CornerRadius="8"
                BorderThickness="2">
            <Border.BorderBrush>
                <SolidColorBrush x:Name="BorderBrush" Color="#007ACC"/>
            </Border.BorderBrush>
        </Border>
        
        <!-- Header 區域 (可拖曳) -->
        <Border x:Name="HeaderBorder"
                VerticalAlignment="Top"
                Height="28"
                CornerRadius="6,6,0,0"
                Cursor="SizeAll">
            <Border.Background>
                <SolidColorBrush x:Name="HeaderBrush" Color="#007ACC"/>
            </Border.Background>
            
            <DockPanel Margin="10,0">
                <!-- Frame 圖示 -->
                <TextBlock Text="▣" 
                           DockPanel.Dock="Left"
                           Foreground="White"
                           FontSize="12"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0"/>
                
                <!-- Frame 標題 -->
                <TextBlock x:Name="TitleLabel"
                           Text="{Binding Name}"
                           Foreground="White"
                           FontWeight="SemiBold"
                           FontSize="12"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
            </DockPanel>
        </Border>
        
        <!-- 選取指示器 -->
        <Border x:Name="SelectionIndicator"
                CornerRadius="8"
                BorderThickness="3"
                BorderBrush="#FFFF00"
                Visibility="Collapsed"/>
    </Grid>
</UserControl>
