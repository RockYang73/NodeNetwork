<UserControl x:Class="ActuatorApp.Views.ParameterNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ActuatorApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="120">
    <Grid MinWidth="80" MinHeight="40">
        <!-- 黃色橢圓背景 -->
        <Ellipse Fill="#FFC107" Stroke="#FF8F00" StrokeThickness="2"/>
        
        <!-- 內容 -->
        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center" 
                    Margin="15,5">
            <!-- 參數名稱 -->
            <TextBlock x:Name="ParamName" 
                       FontWeight="Bold" 
                       FontSize="14"
                       Foreground="#333333"
                       VerticalAlignment="Center" 
                       Margin="0,0,5,0"/>
            
            <!-- 編輯器 (TextBox) -->
            <TextBox x:Name="ValueEditor" 
                     MinWidth="40" 
                     MaxWidth="60"
                     VerticalAlignment="Center"
                     HorizontalContentAlignment="Center"
                     BorderThickness="1"
                     BorderBrush="#FF8F00"
                     Background="White"/>
        </StackPanel>
    </Grid>
</UserControl>
